# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом
- src/components/models/ — папка с классами моделей данных
- src/types/ — папка с типами TypeScript

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Типы данных

Файл `src/types/index.ts` содержит основные типы данных приложения:

### `IProduct` - интерфейс для описания товара
```typescript
interface IProduct {
  id: string;       // уникальный идентификатор товара
  title: string;    // название товара
  description: string; // описание товара
  image: string;    // ссылка на изображение товара
  category: string; // категория, к которой относится товар
  price: number | null; // цена товара (null, если цена не указана)
}
```

### `IBuyer` - интерфейс для данных покупателя
```typescript
interface IBuyer {
  payment: TPayment; // выбранный способ оплаты
  email: string;     // email покупателя
  phone: string;     // телефон покупателя
  address: string;   // адрес доставки
}
```

### `IOrder` - интерфейс для отправки заказа на сервер
```typescript
interface IOrder {
  buyer: IBuyer;     // данные покупателя
  items: IProduct[]; // список выбранных товаров
}
```

### `TPayment` - тип для способов оплаты
```typescript
type TPayment = 'card' | 'cash';
```

### Классы моделей данных

#### Класс `Catalog`
Управляет списком товаров и выбранным товаром в каталоге.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий при изменении данных.

Поля класса:
`products: IProduct[]` - массив товаров в каталоге (инициализируется пустым массивом)
`selectedProduct: IProduct | null` - текущий выбранный товар (инициализируется null)
`events: EventEmitter` - экземпляр EventEmitter для генерации событий

Методы класса:
`setProducts(products: IProduct[]): void` - заменяет текущий список товаров новым массивом и эмитит событие `catalog:change`
`getProducts(): IProduct[]` - возвращает все товары каталога
`getProductById(id: string): IProduct | undefined` - находит товар по его идентификатору
`setSelectedProduct(product: IProduct): void` - устанавливает текущий выбранный товар и эмитит событие `catalog:product-selected`
`getSelectedProduct(): IProduct | null` - возвращает выбранный товар

События:
- `catalog:change` - эмитируется при изменении списка товаров
- `catalog:product-selected` - эмитируется при выборе товара

#### Класс `Cart`
Управляет корзиной товаров: добавлением, удалением, подсчетом стоимости и количества.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий при изменении данных.

Поля класса:
`items: IProduct[]` - массив товаров в корзине (инициализируется пустым массивом)
`events: EventEmitter` - экземпляр EventEmitter для генерации событий

Методы класса:
`getItems(): IProduct[]` - возвращает все товары в корзине
`addItem(product: IProduct): void` - добавляет товар в корзину и эмитит событие `cart:change`
`removeItem(product: IProduct): void` - удаляет товар из корзины и эмитит событие `cart:change`
`clear(): void` - полностью очищает корзину и эмитит событие `cart:change`
`getTotalPrice(): number` - вычисляет общую стоимость всех товаров в корзине
`getCount(): number` - возвращает количество товаров в корзине
`hasItem(productId: string): boolean` - проверяет наличие товара в корзине по его ID

События:
- `cart:change` - эмитируется при любом изменении корзины (добавление, удаление, очистка)

#### Класс `Buyer`
Управляет данными покупателя: способом оплаты, контактной информацией и адресом доставки.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий при изменении данных.

Поля класса:
`payment: TPayment` - выбранный способ оплаты (инициализируется пустой строкой)
`email: string` - email покупателя (инициализируется пустой строкой)
`phone: string` - телефон покупателя (инициализируется пустой строкой)
`address: string` - адрес доставки (инициализируется пустой строкой)
`events: EventEmitter` - экземпляр EventEmitter для генерации событий

Методы класса:
`setData(data: Partial<IBuyer>): void` - устанавливает данные покупателя, позволяя обновлять отдельные поля без потери остальных данных, и эмитит событие `buyer:change`
`setField<K extends keyof IBuyer>(key: K, value: IBuyer[K]): void` - устанавливает одно поле покупателя и эмитит событие `buyer:change`
`getData(): IBuyer` - возвращает полные данные покупателя
`clear(): void` - очищает все данные покупателя, устанавливая пустые значения, и эмитит событие `buyer:change`
`validate(): Partial<Record<keyof IBuyer, string>>` - проверяет валидность данных и возвращает объект с сообщениями об ошибках для невалидных полей. Проверяет формат email (регулярное выражение) и длину телефона (минимум 10 цифр после удаления всех нецифровых символов)

События:
- `buyer:change` - эмитируется при любом изменении данных покупателя

#### Класс Communication
Обеспечивает взаимодействие с сервером для получения товаров и отправки заказов.

Конструктор:
`constructor(api: IApi)` - принимает объект, реализующий интерфейс IApi.

Поля класса:
`api: IApi` - объект для выполнения HTTP-запросов

Методы класса:
`getProductList(): Promise<IProduct[]>` - получает массив товаров с сервера
`sendOrder(order: IOrder): Promise<{ id: string; total: number }>` - отправляет данные заказа на сервер и возвращает информацию о созданном заказе

### Presenter
AppPresenter — это центральный класс приложения, реализующий паттерн MVP (Model-View-Presenter). Он выступает в роли координатора, который:

1. Связывает слои - соединяет Model (данные) с View (интерфейс)
2. Обрабатывает бизнес-логику - управляет потоком данных и состоянием приложения
3. Координирует события - слушает события от View и обновляет Model/View соответственно
4. Управляет жизненным циклом - инициализирует приложение и управляет переходами между состояниями

### Классы представления (Views)

Классы представления отвечают за отображение данных на странице и обработку пользовательских взаимодействий. Все представления используют EventEmitter для генерации событий, которые обрабатываются Presenter'ом.

#### Класс `BasketCounterView`
Отображает счетчик количества товаров в корзине в шапке сайта.

Конструктор:
`constructor(selector: string = '.header__basket-counter')` - принимает селектор DOM-элемента для отображения счетчика.

Поля класса:
`element: HTMLElement` - ссылка на DOM-элемент счетчика

Методы класса:
`update(count: number): void` - обновляет значение счетчика и управляет его видимостью (скрывает, если count = 0)

#### Класс `CardView`
Отображает карточку товара в каталоге. Каждая карточка является кликабельной кнопкой.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLButtonElement` - корневой элемент карточки (кнопка)
`titleEl: HTMLElement` - элемент для отображения названия товара
`priceEl: HTMLElement` - элемент для отображения цены
`categoryEl: HTMLElement` - элемент для отображения категории
`imageEl: HTMLImageElement` - элемент изображения товара

Методы класса:
`render(data: CardViewData): HTMLButtonElement` - заполняет карточку данными товара и возвращает элемент. При клике эмитит событие `card:select` с id товара
`getElement(): HTMLButtonElement` - возвращает корневой элемент карточки
`private setCategory(category: string): void` - устанавливает текст и CSS-класс категории

Интерфейс `CardViewData`:
```typescript
interface CardViewData {
  id: string;
  title: string;
  price: number | null;
  category: string;
  image: string;
}
```

События:
- `card:select` (payload: `{ id: string }`) - эмитируется при клике на карточку

#### Класс `CartItemView`
Отображает отдельный элемент списка товаров в корзине.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLLIElement` - корневой элемент элемента списка
`indexEl: HTMLElement` - элемент для отображения порядкового номера
`titleEl: HTMLElement` - элемент для отображения названия товара
`priceEl: HTMLElement` - элемент для отображения цены
`deleteButton: HTMLElement` - кнопка удаления товара из корзины

Методы класса:
`render(product: IProduct, index: number): HTMLLIElement` - заполняет элемент данными товара и устанавливает обработчик удаления, возвращает элемент
`getElement(): HTMLLIElement` - возвращает корневой элемент

События:
- `cart:remove` (payload: `{ product: IProduct }`) - эмитируется при клике на кнопку удаления

#### Класс `CartView`
Отображает модальное окно корзины с списком товаров, общей стоимостью и кнопкой оформления заказа.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLElement` - корневой элемент корзины
`listEl: HTMLElement` - контейнер для списка товаров
`totalEl: HTMLElement` - элемент для отображения общей стоимости
`orderButton: HTMLButtonElement` - кнопка оформления заказа

Методы класса:
`getElement(): HTMLElement` - возвращает корневой элемент корзины

Сеттеры (используются для обновления представления из Presenter'а):
`set items(items: HTMLElement[]): void` - устанавливает готовую разметку списка товаров
`set total(total: number): void` - устанавливает общую стоимость корзины
`set orderButtonDisabled(disabled: boolean): void` - управляет состоянием кнопки заказа (включает/отключает)

События:
- `cart:order` - эмитируется при клике на кнопку оформления заказа

#### Класс `CatalogView`
Контейнер для отображения каталога карточек товаров.

Конструктор:
`constructor(containerSelector: string)` - принимает селектор DOM-элемента-контейнера для каталога.

Поля класса:
`container: HTMLElement` - контейнер для карточек товаров

Методы класса:
`set items(items: HTMLElement[]): void` - устанавливает готовую разметку списка карточек (очищает контейнер и добавляет новые элементы)

#### Класс `ContactsFormView`
Отображает форму контактов (шаг 2 оформления заказа) с полями для email и телефона.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLFormElement` - корневой элемент формы
`emailInput: HTMLInputElement` - поле ввода email
`phoneInput: HTMLInputElement` - поле ввода телефона
`errorsEl: HTMLElement` - элемент для отображения ошибок валидации
`submitBtn: HTMLButtonElement` - кнопка отправки формы

Методы класса:
`getElement(): HTMLFormElement` - возвращает корневой элемент формы

Сеттеры (используются для обновления представления из Presenter'а):
`set email(value: string): void` - устанавливает значение поля email
`set phone(value: string): void` - устанавливает значение поля телефона
`set errors(errors: string[]): void` - устанавливает список ошибок валидации для отображения
`set valid(isValid: boolean): void` - управляет состоянием кнопки отправки формы

События:
- `buyer:field-change` (payload: `{ key: 'email' | 'phone', value: string }`) - эмитируется при изменении полей формы
- `order:confirm` - эмитируется при отправке формы

#### Класс `HeaderView`
Управляет элементами шапки сайта: кнопкой корзины и счетчиком товаров.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`basketButton: HTMLButtonElement` - кнопка открытия корзины
`basketCounter: BasketCounterView` - экземпляр счетчика корзины

Методы класса:
`updateCounter(count: number): void` - обновляет значение счетчика корзины

События:
- `basket:open` - эмитируется при клике на кнопку корзины

#### Класс `ModalView`
Универсальное модальное окно для отображения различных контентов (превью товара, корзина, формы заказа и т.д.).

Конструктор:
`constructor(_events: EventEmitter, rootOrSelector: HTMLElement | string = '#modal-container')` - принимает экземпляр EventEmitter и селектор или элемент модального окна.

Поля класса:
`root: HTMLElement` - корневой элемент модального окна
`contentContainer: HTMLElement` - контейнер для контента модального окна
`closeButton: HTMLButtonElement | null` - кнопка закрытия (если присутствует в разметке)
`escHandler: (e: KeyboardEvent) => void` - обработчик нажатия клавиши Escape

Методы класса:
`open(contentEl: HTMLElement): void` - открывает модальное окно с указанным контентом, блокирует прокрутку страницы, добавляет обработчик клавиши Escape
`close(): void` - закрывает модальное окно, очищает контент, восстанавливает прокрутку страницы

Приватные методы:
`private initListeners(): void` - инициализирует обработчики событий (клик по оверлею и кнопке закрытия)
`private onOverlayClick(e: MouseEvent): void` - обрабатывает клик по фону модального окна
`private onCloseClick(): void` - обрабатывает клик по кнопке закрытия
`private onEsc(e: KeyboardEvent): void` - обрабатывает нажатие клавиши Escape

#### Класс `OrderFormView`
Отображает форму выбора способа оплаты и адреса доставки (шаг 1 оформления заказа).

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLFormElement` - корневой элемент формы
`btnCard: HTMLButtonElement` - кнопка выбора оплаты картой
`btnCash: HTMLButtonElement` - кнопка выбора оплаты наличными
`addressInput: HTMLInputElement` - поле ввода адреса доставки
`submitBtn: HTMLButtonElement` - кнопка отправки формы
`errorEl: HTMLElement` - элемент для отображения ошибок валидации

Методы класса:
`getElement(): HTMLElement` - возвращает корневой элемент формы

Сеттеры (используются для обновления представления из Presenter'а):
`set payment(value: Payment): void` - устанавливает выбранный способ оплаты и обновляет визуальное состояние кнопок
`set address(value: string): void` - устанавливает значение поля адреса
`set errors(errors: string[]): void` - устанавливает список ошибок валидации для отображения
`set valid(isValid: boolean): void` - управляет состоянием кнопки отправки формы

Приватные методы:
`private setPayment(payment: Payment): void` - обновляет визуальное состояние кнопок выбора способа оплаты

События:
- `buyer:field-change` (payload: `{ key: 'payment' | 'address', value: string }`) - эмитируется при изменении полей формы
- `order:next` - эмитируется при отправке формы

#### Класс `ProductPreviewView`
Отображает детальное превью товара в модальном окне с возможностью добавления/удаления из корзины.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLElement` - корневой элемент превью
`titleEl: HTMLElement` - элемент для отображения названия товара
`descriptionEl: HTMLElement` - элемент для отображения описания товара
`priceEl: HTMLElement` - элемент для отображения цены
`categoryEl: HTMLElement` - элемент для отображения категории
`imageEl: HTMLImageElement` - элемент изображения товара
`buttonEl: HTMLButtonElement` - кнопка добавления/удаления из корзины

Методы класса:
`render(product: IProduct, inCart: boolean): HTMLElement` - заполняет превью данными товара и обновляет состояние кнопки в зависимости от наличия товара в корзине, возвращает элемент
`getElement(): HTMLElement` - возвращает корневой элемент превью

Приватные методы:
`private setPrice(price: number | null): void` - устанавливает цену товара (если null, отображает "Бесценно")
`private updateButton(price: number | null, inCart: boolean): void` - обновляет текст и состояние кнопки в зависимости от цены и наличия товара в корзине

События:
- `preview:toggle` - эмитируется при клике на кнопку добавления/удаления товара из корзины

#### Класс `SuccessView`
Отображает окно успешного оформления заказа с информацией о списанной сумме.

Конструктор:
`constructor(events: EventEmitter)` - принимает экземпляр EventEmitter для генерации событий.

Поля класса:
`element: HTMLElement` - корневой элемент окна успеха
`descriptionEl: HTMLElement` - элемент для отображения описания с суммой заказа
`closeButton: HTMLButtonElement` - кнопка закрытия окна

Методы класса:
`render(total: number): HTMLElement` - заполняет окно суммой заказа и возвращает элемент

События:
- `success:close` - эмитируется при клике на кнопку закрытия

#### Принципы работы классов представления:
- Все классы представления не хранят состояние данных - они только отображают переданные данные
- Валидация выполняется в моделях, представления только отображают ошибки через сеттеры
- Представления эмитируют события при пользовательских взаимодействиях, но не обрабатывают события
- Обновление представлений происходит через сеттеры, вызываемые из Presenter'а
- Каждый класс отвечает за свой блок интерфейса и не зависит от других представлений

### Events
Для событийного взаимодействия между слоями приложения используется собственная реализация брокера событий EventEmitter (файл Events.ts). Этот паттерн "Наблюдатель" позволяет максимально изолировать модули друг от друга и реализовать архитектуру в стиле Publisher–Subscriber.
#### Основные возможности:
##### Подписка на события
```typescript
on<T extends object>(eventName: EventName, callback: (event: T) => void): void
```
- `eventName`: название события (строка или RegExp)
- `callback`: функция-обработчик, получающая данные события
- Поддерживает типизацию через дженерик `<T>`
##### Отписка от событий
```typescript
off(eventName: EventName, callback: Subscriber): void
```
- Удаляет конкретный обработчик для указанного события
- Автоматически очищает пустые коллекции подписчиков

#### Генерация событий
```typescript
emit<T extends object>(eventName: string, data?: T): void
```
- Инициирует событие с передачей опциональных данных
- Уведомляет всех подписчиков на это событие
- Поддерживает подписку по RegExp-шаблонам